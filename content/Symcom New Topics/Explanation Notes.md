Dated: 02nd of January, 2026

```
Database and files backup in server.

Last 5 snapshots saved in server.

Always use development.
```

```javascript
//old comparison webpage connection loading process:

ini = [a,b,c]

loadConnectionsForSymptoms(){
	// step 1
	ini[] => symptom-connection-operations.php
	
	// step 2
	// once response is received from php script
	foreach response{
		connectSave() function is called.
	}
}

Inside symptom-connection-operations.php {
	array = []
	foreach {
		ini => connections table fetch, arrayFetch
		array => arrayFetch.
	}
	
	return array = [
		"1" : 1st initial fetched  [... , ..., ...],
		"2" : 1st initial fetched  [... , ..., ...],
		"3" : 1st initial fetched  [... , ..., ...],
	]
}

Inside connectSave(){
	// 1. connectionType input parameter is checked
	// 2. initial symptom html element is made
	// 3. comparing symptom html element is made
	// 4. the initial and comparative html element is added to the webpage using jQuery
}
```

### 03.01.2026
```

```